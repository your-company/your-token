{
  "dependencies": {
    "ganache-cli": "^6.7.0",
    "openzeppelin-solidity": "^2.3.0",
    "truffle": "^5.0.37"
  },
  "scripts": {
    "start:chain": "bash ./scripts/start-blockchain-client.sh",
    "migrate:local": "yarn run truffle migrate --network local --compile-all --reset",
    "migrate:goerli": "yarn run truffle migrate --network goerli --compile-all --reset"
  }
}
